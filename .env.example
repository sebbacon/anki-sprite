# Anki Service Authentication Credentials
# Copy this file to .env and customize as needed

# Basic Auth credentials (plaintext - will be hashed at runtime)
ANKI_AUTH_USERNAME=your_username
ANKI_AUTH_PASSWORD=your_secure_password

# API Key for X-API-Key header authentication
# Generate with: openssl rand -hex 32
ANKI_API_KEY=your_64_character_hex_api_key_here
